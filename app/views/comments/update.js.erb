alert("update");
<% if @comment.persisted? %>
  alert("update");
  $('#comment-display').append('<%= j render @comment %>');
  $('#commentUpdateModal').modal('hide');
<% else %>
  $("#commentForm").html("<%= j render(partial: 'comments/form') %>");
<% end %>
